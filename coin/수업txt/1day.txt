블록체인 개론부터 다시
    탈중앙화 -> 중요기술이나 아직 멀었다?
    분산원장 기술(중앙의 간섭없이 모든 원장을 관리하는 기술)
    분산처리와 암호화, 보안성, 거래과정의 신속성과 투명성 -> 인증과정이 좀 오래걸리는 경우는 있다 
    블록 + 체인 
    
합의 알고리즘 
    작업증명
    지분증명
    위임지분증명

블록의 구성요소
    매직넘버
    사이즈
    헤더
        버전
        이전해쉬블록
        타임스탬프
        난이도(bits)
        nonce -> 4바이트 크기의 nonce
    바디
        거래카운터
        블록생성자 -> 최초거래
        거래......

블록해시
    블록의 이름 정보
    버전, 이전블록 해시, 머클루트, 타임, bits, 논스 정보를 모두 더하여 합을 구한 후 SHA 256으로 변환한값?

머클루트
    블록의 바디부분에 저장된 트랜잭션(거래정보)들의 해시 트리
    값을 통해 트랜잭션의 무결성을 검증할 뿐 아니라 블록의 해시 값을 생성하였기 때문에 블록의 해시의 무결성도 함께 검증
    -> 합의 알고리즘에서 필요
    -> 많을수록 합의 알고리즘이 오래 걸린다?

비트
    난이도 해시 목표 값을 의미하는 지표

nonce
    블록을 만드는 과정에서 해시 값을 구할 때 필요한 재료 역할
    비트 코인에서 작업 증명은 이 target보다 작게 나오는 블록 헤더의 해쉬 값이 되도록 nonce를 구하는 것? 
    만족하는 nonce를 구하게 되면 채굴 성공? 

비트코인 

비잔티움 장군 문제

채굴

기술 원리 

공개키 암호화 

p2p 네트워크 

프로토콜
    합의 방법의 일반적인 용어 

SHA256
    256비트로 구성되며 64자리 문자열을 반환
    표준해시 알고리즘인 SHA-2 계열중 하나 
    단반향(복구 불가)
    안전성

동작 원리 
    dns seed 를 통해서 seed node가 서로 다른 노드들이 연결할수 있게 해준다?
    genesis block(최초의 블록? 노드) 
    -> 공개하려고 seed node를 만듬
    -> 다른 node들은 아직 seed node와 통신이 안됨  
    -> 서로 다른 node들이 통신할 수 있게 만들기 위해서 dns seed 를 통해서 seed node를 찾아서 연결
    참고로 dns seed에는 블록이 없고 node와 seed node 와 싱크해서 seed node에 있다?

    피어 없으면 dns를 통해서 다시 연결?

dns 네임을 치면 그 ip에 맵핑 시켜준다 ?