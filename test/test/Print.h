#pragma once

#include <iostream>

using namespace std;

class CPrint
{
    // private:

public:
    CPrint() {};
    void Print(int i);
    void Print(double f);
    void Print(char* str);

    // ~CPrint() {};
};

